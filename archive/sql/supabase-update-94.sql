-- SUPERSEDED — no longer needed.
--
-- This migration added the "hide unreleased change orders" filter to
-- portal_change_orders. That filter is now built into supabase-update-93.sql
-- itself, so re-running 93 keeps the exclusion. There is nothing to run here.
--
-- (Kept as a placeholder so migration numbering stays continuous.)
SELECT 1;
